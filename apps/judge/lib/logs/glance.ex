defmodule Aggie.Logs.Glance do
  
# -   Glance Image requests
#   -   Successful images creations are only logged with
#     -   State and Size, UUID (project, domain)
#     -   OpenStack Request ID
#   -   Failed attempts are logged with
#     -   State and UUID (project, domain)
#     -   OpenStack Request ID
#     -   Duration
#     -   Name
#     -   Size
#     -   Specs

end
